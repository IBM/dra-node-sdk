"use strict";
/**
 * (C) Copyright IBM Corp. 2025.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
/**
 * IBM OpenAPI SDK Code Generator Version: 3.105.0-3c13b041-20250605-193116
 */
var extend = require("extend");
var ibm_cloud_sdk_core_1 = require("ibm-cloud-sdk-core");
var common_1 = require("../lib/common");
/**
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * API Version: 1.0.0
 */
var ServicebrokerV1 = /** @class */ (function (_super) {
    __extends(ServicebrokerV1, _super);
    /**
     * Construct a ServicebrokerV1 object.
     *
     * @param {Object} options - Options for the service.
     * @param {string} [options.serviceUrl] - The base URL for the service
     * @param {OutgoingHttpHeaders} [options.headers] - Default headers that shall be included with every request to the service.
     * @param {Authenticator} options.authenticator - The Authenticator object used to authenticate requests to the service
     * @constructor
     * @returns {ServicebrokerV1}
     */
    function ServicebrokerV1(options) {
        var _this = this;
        options = options || {};
        _this = _super.call(this, options) || this;
        if (options.serviceUrl) {
            _this.setServiceUrl(options.serviceUrl);
        }
        return _this;
    }
    /*************************
     * Factory method
     ************************/
    /**
     * Constructs an instance of ServicebrokerV1 with passed in options and external configuration.
     *
     * @param {UserOptions} [options] - The parameters to send to the service.
     * @param {string} [options.serviceName] - The name of the service to configure
     * @param {Authenticator} [options.authenticator] - The Authenticator object used to authenticate requests to the service
     * @param {string} [options.serviceUrl] - The base URL for the service
     * @returns {ServicebrokerV1}
     */
    ServicebrokerV1.newInstance = function (options) {
        options = options || {};
        if (!options.serviceName) {
            options.serviceName = this.DEFAULT_SERVICE_NAME;
        }
        if (!options.authenticator) {
            options.authenticator = (0, ibm_cloud_sdk_core_1.getAuthenticatorFromEnvironment)(options.serviceName);
        }
        var service = new ServicebrokerV1(options);
        service.configureService(options.serviceName);
        if (options.serviceUrl) {
            service.setServiceUrl(options.serviceUrl);
        }
        return service;
    };
    /*************************
     * drAutomationConfig
     ************************/
    /**
     * Get current API key.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - instance id of instance to provision.
     * @param {string} [params.acceptLanguage] - The language requested for the return document.
     * @param {string} [params.ifNoneMatch] - ETag for conditional requests (optional).
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<ServicebrokerV1.Response<ServicebrokerV1.ValidationKeyResponse>>}
     */
    ServicebrokerV1.prototype.serviceInstanceGetKeyV1 = function (params) {
        var _params = __assign({}, params);
        var _requiredParams = ['instanceId'];
        var _validParams = ['instanceId', 'acceptLanguage', 'ifNoneMatch', 'signal', 'headers'];
        var _validationErrors = (0, ibm_cloud_sdk_core_1.validateParams)(_params, _requiredParams, _validParams);
        if (_validationErrors) {
            return Promise.reject(_validationErrors);
        }
        var path = {
            'instance_id': _params.instanceId,
        };
        var sdkHeaders = (0, common_1.getSdkHeaders)(ServicebrokerV1.DEFAULT_SERVICE_NAME, 'v1', 'serviceInstanceGetKeyV1');
        var parameters = {
            options: {
                url: '/drautomation/v1/apikey/{instance_id}',
                method: 'GET',
                path: path,
            },
            defaultOptions: extend(true, {}, this.baseOptions, {
                headers: extend(true, sdkHeaders, this.baseOptions.headers, {
                    'Accept': 'application/json',
                    'Accept-Language': _params.acceptLanguage,
                    'If-None-Match': _params.ifNoneMatch,
                }, _params.headers),
                axiosOptions: {
                    signal: _params.signal,
                },
            }),
        };
        return this.createRequest(parameters);
    };
    /**
     * validate key api.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - instance id of instance to provision.
     * @param {string} params.apiKey - The API key to validate.
     * @param {string} [params.acceptLanguage] - The language requested for the return document.
     * @param {string} [params.ifNoneMatch] - ETag for conditional requests (optional).
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<ServicebrokerV1.Response<ServicebrokerV1.ValidationKeyResponse>>}
     */
    ServicebrokerV1.prototype.serviceInstanceValidateKey = function (params) {
        var _params = __assign({}, params);
        var _requiredParams = ['instanceId', 'apiKey'];
        var _validParams = [
            'instanceId',
            'apiKey',
            'acceptLanguage',
            'ifNoneMatch',
            'signal',
            'headers',
        ];
        var _validationErrors = (0, ibm_cloud_sdk_core_1.validateParams)(_params, _requiredParams, _validParams);
        if (_validationErrors) {
            return Promise.reject(_validationErrors);
        }
        var body = {
            'api_key': _params.apiKey,
        };
        var path = {
            'instance_id': _params.instanceId,
        };
        var sdkHeaders = (0, common_1.getSdkHeaders)(ServicebrokerV1.DEFAULT_SERVICE_NAME, 'v1', 'serviceInstanceValidateKey');
        var parameters = {
            options: {
                url: '/drautomation/v1/apikey/{instance_id}',
                method: 'POST',
                body: body,
                path: path,
            },
            defaultOptions: extend(true, {}, this.baseOptions, {
                headers: extend(true, sdkHeaders, this.baseOptions.headers, {
                    'Accept': 'application/json',
                    'Content-Type': 'application/json',
                    'Accept-Language': _params.acceptLanguage,
                    'If-None-Match': _params.ifNoneMatch,
                }, _params.headers),
                axiosOptions: {
                    signal: _params.signal,
                },
            }),
        };
        return this.createRequest(parameters);
    };
    /**
     * update api key.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - instance id of instance to provision.
     * @param {string} params.apiKey -
     * @param {string} [params.acceptLanguage] - The language requested for the return document.
     * @param {string} [params.ifNoneMatch] - ETag for conditional requests (optional).
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<ServicebrokerV1.Response<ServicebrokerV1.ValidationKeyResponse>>}
     */
    ServicebrokerV1.prototype.serviceInstanceUpdateApiKey = function (params) {
        var _params = __assign({}, params);
        var _requiredParams = ['instanceId', 'apiKey'];
        var _validParams = [
            'instanceId',
            'apiKey',
            'acceptLanguage',
            'ifNoneMatch',
            'signal',
            'headers',
        ];
        var _validationErrors = (0, ibm_cloud_sdk_core_1.validateParams)(_params, _requiredParams, _validParams);
        if (_validationErrors) {
            return Promise.reject(_validationErrors);
        }
        var body = {
            'api_key': _params.apiKey,
        };
        var path = {
            'instance_id': _params.instanceId,
        };
        var sdkHeaders = (0, common_1.getSdkHeaders)(ServicebrokerV1.DEFAULT_SERVICE_NAME, 'v1', 'serviceInstanceUpdateApiKey');
        var parameters = {
            options: {
                url: '/drautomation/v1/apikey/{instance_id}',
                method: 'PUT',
                body: body,
                path: path,
            },
            defaultOptions: extend(true, {}, this.baseOptions, {
                headers: extend(true, sdkHeaders, this.baseOptions.headers, {
                    'Accept': 'application/json',
                    'Content-Type': 'application/json',
                    'Accept-Language': _params.acceptLanguage,
                    'If-None-Match': _params.ifNoneMatch,
                }, _params.headers),
                axiosOptions: {
                    signal: _params.signal,
                },
            }),
        };
        return this.createRequest(parameters);
    };
    /**
     * Get GRS location pairs by instance ID.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - instance id of instance to provision.
     * @param {string} [params.acceptLanguage] - The language requested for the return document.
     * @param {string} [params.ifNoneMatch] - ETag for conditional requests (optional).
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<ServicebrokerV1.Response<ServicebrokerV1.GetGRSLocationPairResponse>>}
     */
    ServicebrokerV1.prototype.drGrsLocationPairsDetails = function (params) {
        var _params = __assign({}, params);
        var _requiredParams = ['instanceId'];
        var _validParams = ['instanceId', 'acceptLanguage', 'ifNoneMatch', 'signal', 'headers'];
        var _validationErrors = (0, ibm_cloud_sdk_core_1.validateParams)(_params, _requiredParams, _validParams);
        if (_validationErrors) {
            return Promise.reject(_validationErrors);
        }
        var path = {
            'instance_id': _params.instanceId,
        };
        var sdkHeaders = (0, common_1.getSdkHeaders)(ServicebrokerV1.DEFAULT_SERVICE_NAME, 'v1', 'drGrsLocationPairsDetails');
        var parameters = {
            options: {
                url: '/drautomation/v1/dr_grs_location_pairs/{instance_id}',
                method: 'GET',
                path: path,
            },
            defaultOptions: extend(true, {}, this.baseOptions, {
                headers: extend(true, sdkHeaders, this.baseOptions.headers, {
                    'Accept': 'application/json',
                    'Accept-Language': _params.acceptLanguage,
                    'If-None-Match': _params.ifNoneMatch,
                }, _params.headers),
                axiosOptions: {
                    signal: _params.signal,
                },
            }),
        };
        return this.createRequest(parameters);
    };
    /**
     * Get current API key.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - instance id of instance to provision.
     * @param {string} [params.acceptLanguage] - The language requested for the return document.
     * @param {string} [params.ifNoneMatch] - ETag for conditional requests (optional).
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<ServicebrokerV1.Response<ServicebrokerV1.GetDrLocationsResponse>>}
     */
    ServicebrokerV1.prototype.serviceInstanceGetdrlocations = function (params) {
        var _params = __assign({}, params);
        var _requiredParams = ['instanceId'];
        var _validParams = ['instanceId', 'acceptLanguage', 'ifNoneMatch', 'signal', 'headers'];
        var _validationErrors = (0, ibm_cloud_sdk_core_1.validateParams)(_params, _requiredParams, _validParams);
        if (_validationErrors) {
            return Promise.reject(_validationErrors);
        }
        var path = {
            'instance_id': _params.instanceId,
        };
        var sdkHeaders = (0, common_1.getSdkHeaders)(ServicebrokerV1.DEFAULT_SERVICE_NAME, 'v1', 'serviceInstanceGetdrlocations');
        var parameters = {
            options: {
                url: '/drautomation/v1/dr_locations/{instance_id}',
                method: 'GET',
                path: path,
            },
            defaultOptions: extend(true, {}, this.baseOptions, {
                headers: extend(true, sdkHeaders, this.baseOptions.headers, {
                    'Accept': 'application/json',
                    'Accept-Language': _params.acceptLanguage,
                    'If-None-Match': _params.ifNoneMatch,
                }, _params.headers),
                axiosOptions: {
                    signal: _params.signal,
                },
            }),
        };
        return this.createRequest(parameters);
    };
    /**
     * Get managed VMs by instance ID.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - instance id of instance to provision.
     * @param {string} [params.acceptLanguage] - The language requested for the return document.
     * @param {string} [params.ifNoneMatch] - ETag for conditional requests (optional).
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<ServicebrokerV1.Response<ServicebrokerV1.ManagedVmListResponse>>}
     */
    ServicebrokerV1.prototype.drManagedVmsDetails = function (params) {
        var _params = __assign({}, params);
        var _requiredParams = ['instanceId'];
        var _validParams = ['instanceId', 'acceptLanguage', 'ifNoneMatch', 'signal', 'headers'];
        var _validationErrors = (0, ibm_cloud_sdk_core_1.validateParams)(_params, _requiredParams, _validParams);
        if (_validationErrors) {
            return Promise.reject(_validationErrors);
        }
        var path = {
            'instance_id': _params.instanceId,
        };
        var sdkHeaders = (0, common_1.getSdkHeaders)(ServicebrokerV1.DEFAULT_SERVICE_NAME, 'v1', 'drManagedVmsDetails');
        var parameters = {
            options: {
                url: '/drautomation/v1/dr_managed_vms/{instance_id}',
                method: 'GET',
                path: path,
            },
            defaultOptions: extend(true, {}, this.baseOptions, {
                headers: extend(true, sdkHeaders, this.baseOptions.headers, {
                    'Accept': 'application/json',
                    'Accept-Language': _params.acceptLanguage,
                    'If-None-Match': _params.ifNoneMatch,
                }, _params.headers),
                axiosOptions: {
                    signal: _params.signal,
                },
            }),
        };
        return this.createRequest(parameters);
    };
    /**
     * DR summary details by instanceID.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - instance id of instance to provision.
     * @param {string} [params.acceptLanguage] - The language requested for the return document.
     * @param {string} [params.ifNoneMatch] - ETag for conditional requests (optional).
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<ServicebrokerV1.Response<ServicebrokerV1.DrAutomationGetSummaryResponse>>}
     */
    ServicebrokerV1.prototype.drSummaryDetails = function (params) {
        var _params = __assign({}, params);
        var _requiredParams = ['instanceId'];
        var _validParams = ['instanceId', 'acceptLanguage', 'ifNoneMatch', 'signal', 'headers'];
        var _validationErrors = (0, ibm_cloud_sdk_core_1.validateParams)(_params, _requiredParams, _validParams);
        if (_validationErrors) {
            return Promise.reject(_validationErrors);
        }
        var path = {
            'instance_id': _params.instanceId,
        };
        var sdkHeaders = (0, common_1.getSdkHeaders)(ServicebrokerV1.DEFAULT_SERVICE_NAME, 'v1', 'drSummaryDetails');
        var parameters = {
            options: {
                url: '/drautomation/v1/dr_summary/{instance_id}',
                method: 'GET',
                path: path,
            },
            defaultOptions: extend(true, {}, this.baseOptions, {
                headers: extend(true, sdkHeaders, this.baseOptions.headers, {
                    'Accept': 'application/json',
                    'Accept-Language': _params.acceptLanguage,
                    'If-None-Match': _params.ifNoneMatch,
                }, _params.headers),
                axiosOptions: {
                    signal: _params.signal,
                },
            }),
        };
        return this.createRequest(parameters);
    };
    /**
     * Validate  ClusterType API.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - instance id of instance to provision.
     * @param {string} params.orchestratorClusterType - orchestrator cluster type value.
     * @param {string} [params.acceptLanguage] - The language requested for the return document.
     * @param {string} [params.ifNoneMatch] - ETag for conditional requests (optional).
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<ServicebrokerV1.Response<ServicebrokerV1.ValidateClusterTypeResponse>>}
     */
    ServicebrokerV1.prototype.validateClusterType = function (params) {
        var _params = __assign({}, params);
        var _requiredParams = ['instanceId', 'orchestratorClusterType'];
        var _validParams = [
            'instanceId',
            'orchestratorClusterType',
            'acceptLanguage',
            'ifNoneMatch',
            'signal',
            'headers',
        ];
        var _validationErrors = (0, ibm_cloud_sdk_core_1.validateParams)(_params, _requiredParams, _validParams);
        if (_validationErrors) {
            return Promise.reject(_validationErrors);
        }
        var query = {
            'orchestrator_cluster_type': _params.orchestratorClusterType,
        };
        var path = {
            'instance_id': _params.instanceId,
        };
        var sdkHeaders = (0, common_1.getSdkHeaders)(ServicebrokerV1.DEFAULT_SERVICE_NAME, 'v1', 'validateClusterType');
        var parameters = {
            options: {
                url: '/drautomation/v1/validate_cluster_type/{instance_id}',
                method: 'GET',
                qs: query,
                path: path,
            },
            defaultOptions: extend(true, {}, this.baseOptions, {
                headers: extend(true, sdkHeaders, this.baseOptions.headers, {
                    'Accept': 'application/json',
                    'Accept-Language': _params.acceptLanguage,
                    'If-None-Match': _params.ifNoneMatch,
                }, _params.headers),
                axiosOptions: {
                    signal: _params.signal,
                },
            }),
        };
        return this.createRequest(parameters);
    };
    /*************************
     * drAutomationIbmCloud
     ************************/
    /**
     * Get MachineTypes by Instance Id.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - instance id of instance to provision.
     * @param {string} params.primaryWorkspaceName - Primary Workspace Name.
     * @param {string} [params.acceptLanguage] - The language requested for the return document.
     * @param {string} [params.ifNoneMatch] - ETag for conditional requests (optional).
     * @param {string} [params.standbyWorkspaceName] - Standby Workspace Name.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<ServicebrokerV1.Response<ServicebrokerV1.MachineTypesByWorkspace>>}
     */
    ServicebrokerV1.prototype.machinetypesDetails = function (params) {
        var _params = __assign({}, params);
        var _requiredParams = ['instanceId', 'primaryWorkspaceName'];
        var _validParams = [
            'instanceId',
            'primaryWorkspaceName',
            'acceptLanguage',
            'ifNoneMatch',
            'standbyWorkspaceName',
            'signal',
            'headers',
        ];
        var _validationErrors = (0, ibm_cloud_sdk_core_1.validateParams)(_params, _requiredParams, _validParams);
        if (_validationErrors) {
            return Promise.reject(_validationErrors);
        }
        var query = {
            'primary_workspace_name': _params.primaryWorkspaceName,
            'standby_workspace_name': _params.standbyWorkspaceName,
        };
        var path = {
            'instance_id': _params.instanceId,
        };
        var sdkHeaders = (0, common_1.getSdkHeaders)(ServicebrokerV1.DEFAULT_SERVICE_NAME, 'v1', 'machinetypesDetails');
        var parameters = {
            options: {
                url: '/drautomation/v1/machinetypes/{instance_id}',
                method: 'GET',
                qs: query,
                path: path,
            },
            defaultOptions: extend(true, {}, this.baseOptions, {
                headers: extend(true, sdkHeaders, this.baseOptions.headers, {
                    'Accept': 'application/json',
                    'Accept-Language': _params.acceptLanguage,
                    'If-None-Match': _params.ifNoneMatch,
                }, _params.headers),
                axiosOptions: {
                    signal: _params.signal,
                },
            }),
        };
        return this.createRequest(parameters);
    };
    /**
     * schematics workspace API.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - instance id of instance to provision.
     * @param {string} [params.ifNoneMatch] - ETag for conditional requests (optional).
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<ServicebrokerV1.Response<ServicebrokerV1.SchematicWorkspacesResponse>>}
     */
    ServicebrokerV1.prototype.schematicWorkspaceGetoperation = function (params) {
        var _params = __assign({}, params);
        var _requiredParams = ['instanceId'];
        var _validParams = ['instanceId', 'ifNoneMatch', 'signal', 'headers'];
        var _validationErrors = (0, ibm_cloud_sdk_core_1.validateParams)(_params, _requiredParams, _validParams);
        if (_validationErrors) {
            return Promise.reject(_validationErrors);
        }
        var path = {
            'instance_id': _params.instanceId,
        };
        var sdkHeaders = (0, common_1.getSdkHeaders)(ServicebrokerV1.DEFAULT_SERVICE_NAME, 'v1', 'schematicWorkspaceGetoperation');
        var parameters = {
            options: {
                url: '/drautomation/v1/schematics_workspaces/{instance_id}',
                method: 'GET',
                path: path,
            },
            defaultOptions: extend(true, {}, this.baseOptions, {
                headers: extend(true, sdkHeaders, this.baseOptions.headers, {
                    'Accept': 'application/json',
                    'If-None-Match': _params.ifNoneMatch,
                }, _params.headers),
                axiosOptions: {
                    signal: _params.signal,
                },
            }),
        };
        return this.createRequest(parameters);
    };
    /**
     * Validate powervs workspace API.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - instance id of instance to provision.
     * @param {string} params.workspaceId - standBy workspaceID value.
     * @param {string} params.crn - crn value.
     * @param {string} params.locationUrl - schematic_workspace_id value.
     * @param {string} [params.ifNoneMatch] - ETag for conditional requests (optional).
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<ServicebrokerV1.Response<ServicebrokerV1.ValidateWorkspaceResponse>>}
     */
    ServicebrokerV1.prototype.validatePowerVsWorkspace = function (params) {
        var _params = __assign({}, params);
        var _requiredParams = ['instanceId', 'workspaceId', 'crn', 'locationUrl'];
        var _validParams = [
            'instanceId',
            'workspaceId',
            'crn',
            'locationUrl',
            'ifNoneMatch',
            'signal',
            'headers',
        ];
        var _validationErrors = (0, ibm_cloud_sdk_core_1.validateParams)(_params, _requiredParams, _validParams);
        if (_validationErrors) {
            return Promise.reject(_validationErrors);
        }
        var query = {
            'workspace_id': _params.workspaceId,
            'crn': _params.crn,
            'location_url': _params.locationUrl,
        };
        var path = {
            'instance_id': _params.instanceId,
        };
        var sdkHeaders = (0, common_1.getSdkHeaders)(ServicebrokerV1.DEFAULT_SERVICE_NAME, 'v1', 'validatePowerVsWorkspace');
        var parameters = {
            options: {
                url: '/drautomation/v1/validate_power_vs_workspace/{instance_id}',
                method: 'GET',
                qs: query,
                path: path,
            },
            defaultOptions: extend(true, {}, this.baseOptions, {
                headers: extend(true, sdkHeaders, this.baseOptions.headers, {
                    'Accept': 'application/json',
                    'If-None-Match': _params.ifNoneMatch,
                }, _params.headers),
                axiosOptions: {
                    signal: _params.signal,
                },
            }),
        };
        return this.createRequest(parameters);
    };
    /**
     * Validate proxyip.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - instance id of instance to provision.
     * @param {string} params.proxyip - proxyip value.
     * @param {string} params.vpcLocation - vpc location value.
     * @param {string} params.vpcId - vpc id value.
     * @param {string} [params.ifNoneMatch] - ETag for conditional requests (optional).
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<ServicebrokerV1.Response<ServicebrokerV1.ValidateProxyipResponse>>}
     */
    ServicebrokerV1.prototype.validateProxyip = function (params) {
        var _params = __assign({}, params);
        var _requiredParams = ['instanceId', 'proxyip', 'vpcLocation', 'vpcId'];
        var _validParams = [
            'instanceId',
            'proxyip',
            'vpcLocation',
            'vpcId',
            'ifNoneMatch',
            'signal',
            'headers',
        ];
        var _validationErrors = (0, ibm_cloud_sdk_core_1.validateParams)(_params, _requiredParams, _validParams);
        if (_validationErrors) {
            return Promise.reject(_validationErrors);
        }
        var query = {
            'proxyip': _params.proxyip,
            'vpc_location': _params.vpcLocation,
            'vpc_id': _params.vpcId,
        };
        var path = {
            'instance_id': _params.instanceId,
        };
        var sdkHeaders = (0, common_1.getSdkHeaders)(ServicebrokerV1.DEFAULT_SERVICE_NAME, 'v1', 'validateProxyip');
        var parameters = {
            options: {
                url: '/drautomation/v1/validate_proxyip/{instance_id}',
                method: 'GET',
                qs: query,
                path: path,
            },
            defaultOptions: extend(true, {}, this.baseOptions, {
                headers: extend(true, sdkHeaders, this.baseOptions.headers, {
                    'Accept': 'application/json',
                    'If-None-Match': _params.ifNoneMatch,
                }, _params.headers),
                axiosOptions: {
                    signal: _params.signal,
                },
            }),
        };
        return this.createRequest(parameters);
    };
    /**
     * workspace API.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - instance id of instance to provision.
     * @param {string} params.locationId - Location ID value.
     * @param {string} params.vpcId - vpc id value.
     * @param {string} params.tgId - transit gateway id value.
     * @param {string} [params.ifNoneMatch] - ETag for conditional requests (optional).
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<ServicebrokerV1.Response<ServicebrokerV1.DRData>>}
     */
    ServicebrokerV1.prototype.workspaceVpcgetoperation = function (params) {
        var _params = __assign({}, params);
        var _requiredParams = ['instanceId', 'locationId', 'vpcId', 'tgId'];
        var _validParams = [
            'instanceId',
            'locationId',
            'vpcId',
            'tgId',
            'ifNoneMatch',
            'signal',
            'headers',
        ];
        var _validationErrors = (0, ibm_cloud_sdk_core_1.validateParams)(_params, _requiredParams, _validParams);
        if (_validationErrors) {
            return Promise.reject(_validationErrors);
        }
        var query = {
            'location_id': _params.locationId,
            'vpc_id': _params.vpcId,
            'tg_id': _params.tgId,
        };
        var path = {
            'instance_id': _params.instanceId,
        };
        var sdkHeaders = (0, common_1.getSdkHeaders)(ServicebrokerV1.DEFAULT_SERVICE_NAME, 'v1', 'workspaceVpcgetoperation');
        var parameters = {
            options: {
                url: '/drautomation/v1/workspaces_custom_vpc/{instance_id}',
                method: 'GET',
                qs: query,
                path: path,
            },
            defaultOptions: extend(true, {}, this.baseOptions, {
                headers: extend(true, sdkHeaders, this.baseOptions.headers, {
                    'Accept': 'application/json',
                    'If-None-Match': _params.ifNoneMatch,
                }, _params.headers),
                axiosOptions: {
                    signal: _params.signal,
                },
            }),
        };
        return this.createRequest(parameters);
    };
    /**
     * workspace API.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - instance id of instance to provision.
     * @param {string} params.schematicId - Schematic ID value.
     * @param {string} params.locationId - Location ID value.
     * @param {string} [params.ifNoneMatch] - ETag for conditional requests (optional).
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<ServicebrokerV1.Response<ServicebrokerV1.DRData>>}
     */
    ServicebrokerV1.prototype.workspaceGetoperation = function (params) {
        var _params = __assign({}, params);
        var _requiredParams = ['instanceId', 'schematicId', 'locationId'];
        var _validParams = [
            'instanceId',
            'schematicId',
            'locationId',
            'ifNoneMatch',
            'signal',
            'headers',
        ];
        var _validationErrors = (0, ibm_cloud_sdk_core_1.validateParams)(_params, _requiredParams, _validParams);
        if (_validationErrors) {
            return Promise.reject(_validationErrors);
        }
        var query = {
            'schematic_id': _params.schematicId,
            'location_id': _params.locationId,
        };
        var path = {
            'instance_id': _params.instanceId,
        };
        var sdkHeaders = (0, common_1.getSdkHeaders)(ServicebrokerV1.DEFAULT_SERVICE_NAME, 'v1', 'workspaceGetoperation');
        var parameters = {
            options: {
                url: '/drautomation/v1/workspaces_schematic/{instance_id}',
                method: 'GET',
                qs: query,
                path: path,
            },
            defaultOptions: extend(true, {}, this.baseOptions, {
                headers: extend(true, sdkHeaders, this.baseOptions.headers, {
                    'Accept': 'application/json',
                    'If-None-Match': _params.ifNoneMatch,
                }, _params.headers),
                axiosOptions: {
                    signal: _params.signal,
                },
            }),
        };
        return this.createRequest(parameters);
    };
    /*************************
     * drAutomationManageDr
     ************************/
    /**
     * DR summary details by instanceID.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - instance id of instance to provision.
     * @param {string} [params.acceptLanguage] - The language requested for the return document.
     * @param {string} [params.ifNoneMatch] - ETag for conditional requests (optional).
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<ServicebrokerV1.Response<ServicebrokerV1.ServiceInstanceManageDR>>}
     */
    ServicebrokerV1.prototype.serviceInstanceFetchManageDr = function (params) {
        var _params = __assign({}, params);
        var _requiredParams = ['instanceId'];
        var _validParams = ['instanceId', 'acceptLanguage', 'ifNoneMatch', 'signal', 'headers'];
        var _validationErrors = (0, ibm_cloud_sdk_core_1.validateParams)(_params, _requiredParams, _validParams);
        if (_validationErrors) {
            return Promise.reject(_validationErrors);
        }
        var path = {
            'instance_id': _params.instanceId,
        };
        var sdkHeaders = (0, common_1.getSdkHeaders)(ServicebrokerV1.DEFAULT_SERVICE_NAME, 'v1', 'serviceInstanceFetchManageDr');
        var parameters = {
            options: {
                url: '/drautomation/v1/manage_dr/{instance_id}',
                method: 'GET',
                path: path,
            },
            defaultOptions: extend(true, {}, this.baseOptions, {
                headers: extend(true, sdkHeaders, this.baseOptions.headers, {
                    'Accept': 'application/json',
                    'Accept-Language': _params.acceptLanguage,
                    'If-None-Match': _params.ifNoneMatch,
                }, _params.headers),
                axiosOptions: {
                    signal: _params.signal,
                },
            }),
        };
        return this.createRequest(parameters);
    };
    /**
     * create manageDR a service instance.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - instance id of instance to provision.
     * @param {string} params.standByRedeploy - Flag to indicate if standby should be redeployed (must be "true" or
     * "false").
     * @param {Context} params.context - Contextual information about the DR configuration including workspace details,
     * orchestrator information, and infrastructure specifications.
     * @param {string} params.planId - The identifier of the plan associated with the service instance.
     * @param {string} params.serviceId - The identifier of the service being managed.
     * @param {string} [params.action] - The action to be performed on the service instance.
     * @param {ManageDrParameters} [params.parameters] - Additional service-specific parameters required for DR
     * configuration. These may vary depending on the service being managed.
     * @param {string} [params.acceptLanguage] - The language requested for the return document.
     * @param {string} [params.ifNoneMatch] - ETag for conditional requests (optional).
     * @param {boolean} [params.acceptsIncomplete] - A value of true indicates that both the IBM Cloud platform and the
     * requesting client support asynchronous deprovisioning.
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<ServicebrokerV1.Response<ServicebrokerV1.ServiceInstanceManageDR>>}
     */
    ServicebrokerV1.prototype.serviceInstanceManageDr = function (params) {
        var _params = __assign({}, params);
        var _requiredParams = ['instanceId', 'standByRedeploy', 'context', 'planId', 'serviceId'];
        var _validParams = [
            'instanceId',
            'standByRedeploy',
            'context',
            'planId',
            'serviceId',
            'action',
            'parameters',
            'acceptLanguage',
            'ifNoneMatch',
            'acceptsIncomplete',
            'signal',
            'headers',
        ];
        var _validationErrors = (0, ibm_cloud_sdk_core_1.validateParams)(_params, _requiredParams, _validParams);
        if (_validationErrors) {
            return Promise.reject(_validationErrors);
        }
        var body = {
            'context': _params.context,
            'plan_id': _params.planId,
            'service_id': _params.serviceId,
            'action': _params.action,
            'parameters': _params.parameters,
        };
        var query = {
            'stand_by_redeploy': _params.standByRedeploy,
            'accepts_incomplete': _params.acceptsIncomplete,
        };
        var path = {
            'instance_id': _params.instanceId,
        };
        var sdkHeaders = (0, common_1.getSdkHeaders)(ServicebrokerV1.DEFAULT_SERVICE_NAME, 'v1', 'serviceInstanceManageDr');
        var parameters = {
            options: {
                url: '/drautomation/v1/manage_dr/{instance_id}',
                method: 'POST',
                body: body,
                qs: query,
                path: path,
            },
            defaultOptions: extend(true, {}, this.baseOptions, {
                headers: extend(true, sdkHeaders, this.baseOptions.headers, {
                    'Accept': 'application/json',
                    'Content-Type': 'application/json',
                    'Accept-Language': _params.acceptLanguage,
                    'If-None-Match': _params.ifNoneMatch,
                }, _params.headers),
                axiosOptions: {
                    signal: _params.signal,
                },
            }),
        };
        return this.createRequest(parameters);
    };
    /*************************
     * drAutomationServiceInstance
     ************************/
    /**
     * dr deployment api.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - instance id of instance to provision.
     * @param {string} [params.ifNoneMatch] - ETag for conditional requests (optional).
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<ServicebrokerV1.Response<ServicebrokerV1.DeploymentStatus>>}
     */
    ServicebrokerV1.prototype.serviceInstanceDrdeployment = function (params) {
        var _params = __assign({}, params);
        var _requiredParams = ['instanceId'];
        var _validParams = ['instanceId', 'ifNoneMatch', 'signal', 'headers'];
        var _validationErrors = (0, ibm_cloud_sdk_core_1.validateParams)(_params, _requiredParams, _validParams);
        if (_validationErrors) {
            return Promise.reject(_validationErrors);
        }
        var path = {
            'instance_id': _params.instanceId,
        };
        var sdkHeaders = (0, common_1.getSdkHeaders)(ServicebrokerV1.DEFAULT_SERVICE_NAME, 'v1', 'serviceInstanceDrdeployment');
        var parameters = {
            options: {
                url: '/drautomation/v1/dr_deployment/{instance_id}',
                method: 'GET',
                path: path,
            },
            defaultOptions: extend(true, {}, this.baseOptions, {
                headers: extend(true, sdkHeaders, this.baseOptions.headers, {
                    'Accept': 'application/json',
                    'If-None-Match': _params.ifNoneMatch,
                }, _params.headers),
                axiosOptions: {
                    signal: _params.signal,
                },
            }),
        };
        return this.createRequest(parameters);
    };
    /**
     * last operation api.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.instanceId - instance id of instance to provision.
     * @param {string} [params.acceptLanguage] - The language requested for the return document.
     * @param {string} [params.ifNoneMatch] - ETag for conditional requests (optional).
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<ServicebrokerV1.Response<ServicebrokerV1.ServiceInstanceStatus>>}
     */
    ServicebrokerV1.prototype.serviceInstanceLastoperation = function (params) {
        var _params = __assign({}, params);
        var _requiredParams = ['instanceId'];
        var _validParams = ['instanceId', 'acceptLanguage', 'ifNoneMatch', 'signal', 'headers'];
        var _validationErrors = (0, ibm_cloud_sdk_core_1.validateParams)(_params, _requiredParams, _validParams);
        if (_validationErrors) {
            return Promise.reject(_validationErrors);
        }
        var path = {
            'instance_id': _params.instanceId,
        };
        var sdkHeaders = (0, common_1.getSdkHeaders)(ServicebrokerV1.DEFAULT_SERVICE_NAME, 'v1', 'serviceInstanceLastoperation');
        var parameters = {
            options: {
                url: '/drautomation/v1/last_operation/{instance_id}',
                method: 'GET',
                path: path,
            },
            defaultOptions: extend(true, {}, this.baseOptions, {
                headers: extend(true, sdkHeaders, this.baseOptions.headers, {
                    'Accept': 'application/json',
                    'Accept-Language': _params.acceptLanguage,
                    'If-None-Match': _params.ifNoneMatch,
                }, _params.headers),
                axiosOptions: {
                    signal: _params.signal,
                },
            }),
        };
        return this.createRequest(parameters);
    };
    /*************************
     * drEvents
     ************************/
    /**
     * Get events from this cloud instance since a specific timestamp.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.provisionId - provision id.
     * @param {string} [params.time] - (deprecated - use from_time) A time in either ISO 8601 or unix epoch format.
     * @param {string} [params.fromTime] - A from query time in either ISO 8601 or unix epoch format.
     * @param {string} [params.toTime] - A to query time in either ISO 8601 or unix epoch format.
     * @param {string} [params.acceptLanguage] - The language requested for the return document.
     * @param {string} [params.ifNoneMatch] - ETag for conditional requests (optional).
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<ServicebrokerV1.Response<ServicebrokerV1.Events>>}
     */
    ServicebrokerV1.prototype.serviceInstanceEventsGetquery = function (params) {
        var _params = __assign({}, params);
        var _requiredParams = ['provisionId'];
        var _validParams = [
            'provisionId',
            'time',
            'fromTime',
            'toTime',
            'acceptLanguage',
            'ifNoneMatch',
            'signal',
            'headers',
        ];
        var _validationErrors = (0, ibm_cloud_sdk_core_1.validateParams)(_params, _requiredParams, _validParams);
        if (_validationErrors) {
            return Promise.reject(_validationErrors);
        }
        var query = {
            'time': _params.time,
            'from_time': _params.fromTime,
            'to_time': _params.toTime,
        };
        var path = {
            'provision_id': _params.provisionId,
        };
        var sdkHeaders = (0, common_1.getSdkHeaders)(ServicebrokerV1.DEFAULT_SERVICE_NAME, 'v1', 'serviceInstanceEventsGetquery');
        var parameters = {
            options: {
                url: '/drautomation/v1/service_instances/{provision_id}/events',
                method: 'GET',
                qs: query,
                path: path,
            },
            defaultOptions: extend(true, {}, this.baseOptions, {
                headers: extend(true, sdkHeaders, this.baseOptions.headers, {
                    'Accept': 'application/json',
                    'Accept-Language': _params.acceptLanguage,
                    'If-None-Match': _params.ifNoneMatch,
                }, _params.headers),
                axiosOptions: {
                    signal: _params.signal,
                },
            }),
        };
        return this.createRequest(parameters);
    };
    /**
     * Get a single event.
     *
     * @param {Object} params - The parameters to send to the service.
     * @param {string} params.provisionId - provision id.
     * @param {string} params.eventId - Event ID.
     * @param {string} [params.acceptLanguage] - The language requested for the return document.
     * @param {string} [params.ifNoneMatch] - ETag for conditional requests (optional).
     * @param {OutgoingHttpHeaders} [params.headers] - Custom request headers
     * @returns {Promise<ServicebrokerV1.Response<ServicebrokerV1.Event>>}
     */
    ServicebrokerV1.prototype.serviceInstanceEventsGet = function (params) {
        var _params = __assign({}, params);
        var _requiredParams = ['provisionId', 'eventId'];
        var _validParams = [
            'provisionId',
            'eventId',
            'acceptLanguage',
            'ifNoneMatch',
            'signal',
            'headers',
        ];
        var _validationErrors = (0, ibm_cloud_sdk_core_1.validateParams)(_params, _requiredParams, _validParams);
        if (_validationErrors) {
            return Promise.reject(_validationErrors);
        }
        var path = {
            'provision_id': _params.provisionId,
            'event_id': _params.eventId,
        };
        var sdkHeaders = (0, common_1.getSdkHeaders)(ServicebrokerV1.DEFAULT_SERVICE_NAME, 'v1', 'serviceInstanceEventsGet');
        var parameters = {
            options: {
                url: '/drautomation/v1/service_instances/{provision_id}/events/{event_id}',
                method: 'GET',
                path: path,
            },
            defaultOptions: extend(true, {}, this.baseOptions, {
                headers: extend(true, sdkHeaders, this.baseOptions.headers, {
                    'Accept': 'application/json',
                    'Accept-Language': _params.acceptLanguage,
                    'If-None-Match': _params.ifNoneMatch,
                }, _params.headers),
                axiosOptions: {
                    signal: _params.signal,
                },
            }),
        };
        return this.createRequest(parameters);
    };
    ServicebrokerV1.DEFAULT_SERVICE_NAME = 'servicebroker';
    return ServicebrokerV1;
}(ibm_cloud_sdk_core_1.BaseService));
/*************************
 * interfaces
 ************************/
(function (ServicebrokerV1) {
    /** Constants for the `serviceInstanceManageDr` operation. */
    var ServiceInstanceManageDrConstants;
    (function (ServiceInstanceManageDrConstants) {
        /** Flag to indicate if standby should be redeployed (must be "true" or "false"). */
        var StandByRedeploy;
        (function (StandByRedeploy) {
            StandByRedeploy["TRUE"] = "true";
            StandByRedeploy["FALSE"] = "false";
        })(StandByRedeploy = ServiceInstanceManageDrConstants.StandByRedeploy || (ServiceInstanceManageDrConstants.StandByRedeploy = {}));
    })(ServiceInstanceManageDrConstants = ServicebrokerV1.ServiceInstanceManageDrConstants || (ServicebrokerV1.ServiceInstanceManageDrConstants = {}));
    var Event;
    (function (Event) {
        var Constants;
        (function (Constants) {
            /** Level of the event (notice, info, warning, error). */
            var Level;
            (function (Level) {
                Level["NOTICE"] = "notice";
                Level["INFO"] = "info";
                Level["WARNING"] = "warning";
                Level["ERROR"] = "error";
            })(Level = Constants.Level || (Constants.Level = {}));
        })(Constants = Event.Constants || (Event.Constants = {}));
    })(Event = ServicebrokerV1.Event || (ServicebrokerV1.Event = {}));
})(ServicebrokerV1 || (ServicebrokerV1 = {}));
module.exports = ServicebrokerV1;
//# sourceMappingURL=v1.js.map